Testing:
Bat, game is already in the list, but still have to select it first to play
.EXE Select but no confirmation after name is filled in DETROIT_BECOME_HUMAN


More translations - help text



Yes this works now.
But I have a few issues I want to discuss and if possible fix or implement.
- I need a button to remove all games or a series of games, if I want to start fresh, else it is adding games.

- When scanning a folder, I take DuckStation as example, it loads and shows a lot of "garbage"
  Some games consist of multiple .bin files, in that case it should only save and show the one .cue file.
  Also when selecting a vivanonno game it does not recognize the .zips in the folder.
  Only after I used show "all types" it sees the .zips but then tries to open the .zip instead of running it.

- The games list becomes quite long is there anything available to quickly find the game you are looking for without impacting performance of the app?

- And something completely different.
  Some games have patches for additional features or just to be able to run the game.
  For instance for a special version of rpcs3 i have such a patch.
  You need patch 1 to load the game with the test menu enabled and patch 2 to disable that patch and load the game normally.
  I also uses files from the "data" folder it needs for the patching.
  But I noticed I don't have a UI to run the game with that patch.
  Here is the PatchServiceTool.ahk to make this use case clearer for you:
